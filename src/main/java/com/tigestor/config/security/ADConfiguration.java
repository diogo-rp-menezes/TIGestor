package com.tigestor.config.security;

import org.springframework.context.annotation.Configuration;

@Configuration
public class ADConfiguration {
    // Configuração futura de autenticação via Active Directory (LDAP)
}